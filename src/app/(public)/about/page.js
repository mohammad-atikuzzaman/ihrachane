import React from 'react';

const About = () => {
    throw new Error("Error in loadign")
    return (
        <div>
            this is About page
        
        </div>
    );
};

export default About;